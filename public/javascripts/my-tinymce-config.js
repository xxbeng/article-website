/* tinymce.init({
    selector: 'textarea#my-expressjs-tinymce-app',
    plugins: 'lists link image table code help wordcount',
    images_upload_url: "uploadImage"
  }); */

/* const { default: tinymce } = require("tinymce"); ----- this was autogenerated?????*/


  const tinyTitleConfig = {
    selector: '#tinyTitle',
    menubar: false,
    inline: true,
    toolbar: false,
    placeholder: "Input title here"
  };

  const tinyContentConfig = {
    selector: '#tinyContent',
    plugins: 'lists link image table code help wordcount preview style table',
    toolbar: 'preview undo redo | styleselect | fontfamily forecolor backcolor align bold italic | link image',
    placeholder: "Input text here",
    images_upload_url: "uploadImage",
    content_style: 'img { max-width: 700px; height: auto; }'
  };

  const tinyCommentConfig = {
    selector: '#tinyComment',
    plugins: "wordcount emoticons autoresize",
    toolbar: "fontfamily bold italic underline forecolor backcolor",
    menubar: false,
    width: '400px',
    height: '400px',
    tinycomments_mode: 'embedded',
    sidebar_show: 'showcomments'
  };

tinymce.init(tinyTitleConfig);
tinymce.init(tinyContentConfig);
tinymce.init(tinyCommentConfig);